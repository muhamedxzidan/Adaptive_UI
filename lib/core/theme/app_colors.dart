import 'package:flutter/material.dart';

class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  static const Color primaryPurple = Color(0xFF2B1A4E);
  static const Color textWhite = Colors.white;
}
